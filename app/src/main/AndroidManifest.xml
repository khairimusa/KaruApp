<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.karu">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:name=".Home"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity android:name=".HomeActivity" android:theme="@style/MyTheme" android:screenOrientation="portrait"></activity>
        <activity android:name=".RegisterActivity" android:screenOrientation="portrait" />
        <activity android:name=".LoginActivity" android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".Screens.Favourite.FavouriteActivity" android:theme="@style/MyTheme"></activity>
        <activity android:name=".Screens.Photo.PhotoActivity" android:theme="@style/MyTheme"></activity>
        <activity android:name=".Screens.Message.MessageActivity" android:theme="@style/MyTheme"></activity>
        <activity android:name=".Screens.Profile.ProfileActivity" android:theme="@style/MyTheme"></activity>
        <activity android:name=".AditionalActivities.SearchActivity" android:theme="@style/MyTheme"></activity>
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>